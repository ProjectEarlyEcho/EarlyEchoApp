#pragma once

// Copy this file to arduino_secrets.h and set the network used by both the
// phone and the ESP32-S3. The real file is ignored by Git.
#define SECRET_WIFI_SSID "your-wifi-name"
#define SECRET_WIFI_PASS "your-wifi-password"
